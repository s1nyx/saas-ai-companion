# After modifying the schema.prisma file, run the following commands to update the database

```bash
npx prisma generate
npx prisma db push
```

# To show the UI studio of prisma, run the following command

```bash
npx prisma studio
```